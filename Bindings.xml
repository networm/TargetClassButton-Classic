<Bindings>
	<Binding name="INSPECT" header="TARGETCLASSBUTTON">
		InspectUnit("target")
	</Binding>
	<Binding name="TRADE">
		InitiateTrade("target")
	</Binding>
	<Binding name="WHISPER">
		local server = nil;
		local name, server = UnitName("target");
		local fullname = name;
		if ( server and (not "target" or UnitRealmRelationship("target") ~= LE_REALM_RELATION_SAME) ) then
			fullname = name.."-"..server;
		end
		ChatFrame_SendTell(fullname)
	</Binding>
	<Binding name="FOLLOW">
		FollowUnit("target")
	</Binding>
	<!--<Binding name="COMPARE_ACHIEVEMENTS">
		InspectAchievements("target")
	</Binding>-->
	<Binding name="Invite">
		InviteUnit("target")
	</Binding>
</Bindings>
